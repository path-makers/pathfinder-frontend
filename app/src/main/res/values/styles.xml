<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TagButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:background">@drawable/tag_background</item>
        <item name="android:textColor">@color/mainColor</item>
    </style>

    <resources>

        <style name="CommonButtonStyle">
            <item name="android:layout_width">match_parent</item>
            <item name="android:layout_height">50dp</item>
            <item name="android:background">@drawable/background_radius_maincolor</item>
            <item name="android:textColor">@color/white</item>
        </style>

    </resources>

</resources>