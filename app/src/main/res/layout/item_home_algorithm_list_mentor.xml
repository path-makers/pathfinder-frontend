<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="170dp"
    android:background="@drawable/item_home_mentee"
    android:layout_margin="8dp"
    android:padding="12dp">

    <TextView
        android:id="@+id/titleAreaAlgorithm"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="제목"
        android:textStyle="bold"
        android:textSize="16sp"
        android:maxLines="1"
        android:lineHeight="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/contentAreaAlgorithm"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/pretendard_semibold"
        android:textColor="@color/black"
        />

    <TextView
        android:id="@+id/contentAreaAlgorithm"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:lineHeight="22dp"
        android:text="aaaaaaaaaaaaaa"
        android:textSize="12sp"
        android:maxLines="3"
        app:layout_constraintTop_toBottomOf="@id/titleAreaAlgorithm"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="4dp"
        android:fontFamily="@font/pretendard_semibold"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
